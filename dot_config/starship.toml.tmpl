format = """
$username $directory( [\\[](bold)$git_branch$git_status$git_metrics$git_state[\\]](bold))
$character"""

add_newline = false

[username]
show_always = true
style_user = "fg:{{ .hex.fg }} bold"
format = "[$user]($style)"

[directory]
format = "[$path](fg:{{ .hex.color8 }})"
truncate_to_repo = true
truncation_length = 0
home_symbol = "~"

[git_branch]
symbol = "git:"
format = "([$symbol$branch](fg:{{ .hex.accent }} bold))"

[git_metrics]
added_style = "fg:{{ .hex.color2 }}"
deleted_style = "fg:{{ .hex.color1 }}"
format = "( ([+$added](fg:{{ .hex.color2 }}))([-$deleted](fg:{{ .hex.color1 }})))"
disabled = false
only_nonzero_diffs = true

[git_status]
style = "fg:{{ .hex.accent }}"
staged = "A$count"
modified = "M$count"
deleted = "D$count"
renamed = "R$count"
typechanged = "T$count"
untracked = "?$count"
stashed = "S$count"
ahead = "ahead:$count"
behind = "behind:$count"
diverged = "ahead:$ahead_count behind:$behind_count"
format = "( [$ahead_behind](fg:{{ .hex.color8 }}))( [$all_status](fg:{{ .hex.color8 }}))"

[git_state]
style = "fg:{{ .hex.color3 }}"
format = "( [$state($progress_current/$progress_total)](fg:{{ .hex.color3 }}))"

[character]
success_symbol = "[>](fg:{{ .hex.accent }} bold)"
error_symbol = "[>](fg:{{ .hex.accent }} bold)"
vimcmd_symbol = "[<](fg:{{ .hex.accent }} bold)"

